<%= form_with(model: organization, local: true, html: { class: "wrap-form-booking" }) do |form| %>
  <% if organization.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(organization.errors.count, "error") %> prohibited this organization from being saved:</h2>

      <ul>
      <% organization.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="container">
    <div class="form-group">
      <%= form.label :name %>
      <%= form.text_area :name %>
    </div>

    <div class="form-group">
      <%= form.label :url %>
      <%= form.text_area :url %>
    </div>

    <div class="form-group">
      <%= form.label :image_url %>
      <%= form.text_area :image_url %>
    </div>

    <div class="form-group">
      <%= form.label :text %>
      <%= form.text_area :text %>
    </div>

    <button type="submit">
      <%= form.submit %>
    </div>
  </div>
<% end %>
